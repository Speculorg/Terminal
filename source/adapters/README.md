# adapters

This directory contains source code for adapters to market data providers and financial functionality (aggregators, exchanges, wallets, etc.).

Each adapter runs in a separate container and communicates with other components through standardized protocols.
