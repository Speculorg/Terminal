# configs

This directory contains the static configuration files for the Terminal project. Here you can find environment variable files, YAML configuration templates, and other necessary settings for running containers and microservices.

Use this directory to store and manage configurations for different environments (develop, release).
