# ELK Module Configuration

This directory contains configuration files for the ELK (Elasticsearch, Logstash, Kibana) infrastructure module.

## Configuration Files
- `config.yml` - Main configuration file for the ELK module

## Environment Variables
Module uses environment variables from:
- `/configs/.env.develop` for development
- `/configs/.env.release` for staging/production 