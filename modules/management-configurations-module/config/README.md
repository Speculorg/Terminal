# Configurations Module Configuration

This directory contains configuration files for the Configurations management module.

## Configuration Files
- `config.yml` - Main configuration file for the Configurations module

## Environment Variables
Module uses environment variables from:
- `/configs/.env.develop` for development
- `/configs/.env.release` for staging/production 