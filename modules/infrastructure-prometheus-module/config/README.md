# Prometheus Module Configuration

This directory contains configuration files for the Prometheus infrastructure module.

## Configuration Files
- `config.yml` - Main configuration file for the Prometheus module

## Environment Variables
Module uses environment variables from:
- `/configs/.env.develop` for development
- `/configs/.env.release` for staging/production 