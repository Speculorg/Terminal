# Audit Module Configuration

This directory contains configuration files for the Audit security module.

## Configuration Files
- `config.yml` - Main configuration file for the Audit module

## Environment Variables
Module uses environment variables from:
- `/configs/.env.develop` for development
- `/configs/.env.release` for staging/production 