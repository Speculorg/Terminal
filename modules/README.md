# modules

This directory contains the source code for the pluggable modules of the Terminal project. Each module implements additional functionality for the system's microservices and adapters.

The modules are designed to be dynamically integrated and scaled, ensuring the flexibility and extensibility of the system.
