# core/net

Net — фасад сетевых проверок ядра.

## Контракт TERM-1
- TCP: `tcp_check`, `tcp_wait`
- HTTP: `http_get`

## Инварианты
- Методы не выбрасывают сетевые исключения наружу.
- Таймауты управляются через конфигурацию (по умолчанию 3000ms).
