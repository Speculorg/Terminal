# Security Service Configuration

This directory contains configuration files for the Security Service microservice.

## Configuration Files
- `config.yml` - Main configuration file for the Security Service

## Environment Variables
Service uses environment variables from:
- `/configs/.env.develop` for development
- `/configs/.env.release` for staging/production 