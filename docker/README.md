# docker

This directory contains files related to the containerization of the Terminal project. It includes:
- Dockerfiles for each managing service and module.
- A docker-compose.yml file for building and running all containers in a unified environment.

Use this directory to manage images, build, and test containers.
