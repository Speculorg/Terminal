# Speculorg.Terminal.Infrastructure.Service Docker Container

This container runs the Django-based infrastructure service that provides core infrastructure functionality for the Speculorg.Terminal project. 