# Speculorg.Terminal.Security.Service Docker Container

This container runs the Django-based security service that provides authentication, authorization, and audit functionality for the Speculorg.Terminal project. 